import React from 'react';

const Timer = (props) => {
    
    return(
    
        <div>
            
            {props.hr} {props.mn} {props.sc}
        
        </div>
        
    )
    
}

export default Timer;